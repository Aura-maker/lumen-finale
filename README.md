# ImparaFacile Backend

Struttura pulita per Render deploy.

## Deploy
- Root Directory: `.`
- Build: `npm install && npx prisma generate`
- Start: `npm start`

## Env
- DATABASE_URL
- PORT=10000
- NODE_ENV=production
